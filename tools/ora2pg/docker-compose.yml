services:
  ora2pg:
    image: georgmoser/ora2pg:latest
    container_name: tipa_ora2pg
    restart: "no"
    network_mode: host          # Linux: container thấy mọi LAN address từ host
    volumes:
      - ./config:/config:ro
      - ./data:/data
    command: tail -f /dev/null
